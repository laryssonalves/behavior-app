import GlobalStyle from '../../../styles/global-style'

export default {
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },

  text: {
    ...GlobalStyle.textMedium,
    fontSize: 16,
    marginTop: 16,
  },
}
